apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: angular-js
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4200
      nodePort: 30008